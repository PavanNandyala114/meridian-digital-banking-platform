server:
  port: 8083

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/meridian_payment?useSSL=false&serverTimezone=UTC
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true